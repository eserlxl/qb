interface:
  display_name: "QB"
  short_description: "Repo-aware planning, autopsy, and audit handoff"
  default_prompt: "Use $qb to inspect this repo, create a main plan, run an existing-project autopsy, create phase sub-plans, a QA audit, and a gated implementation handoff."
